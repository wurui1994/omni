# macOS Finder 的目录元数据，跟仓库无关
.DS_Store

# node
node_modules/
package-lock.json

# `omni build` 的产物：默认输出是去掉后缀的源文件名，没有固定后缀，只能按目录约定挡
/out
/a.out
*.dSYM/

# C 后端的中间产物（正常情况下落在 mktemp 目录里，这几条是手工 emit-c 时的落点）
*.o
/*.c

# js 往返测试重新生成出来的编译器树（留在磁盘上是为了能直接 diff / 直接跑）
/.omni-build/

# `omni bootstrap` 的产物树（C1 + N1 + lib + runtime）。是生成物，不进版本库。
/dist/
