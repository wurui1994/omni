## ext/nim/examples/slice.nim —— 与 go / V / mojo 那几份 slice **同一件事**
##
## 期望输出逐行相同：20 / 30。
## nim 的 `xs[1 .. 2]` 是**含上界**的 —— 图上那格不含，所以映射在那儿 +1。
## 这一条与"下标起点"是同一类：语言之间答案不同的东西，答案由那门语言的映射给。

var xs = @[10, 20, 30, 40]
var ys = xs[1 .. 2]
echo ys[0]
echo ys[1]
