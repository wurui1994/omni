// ext/evaldraw/examples/graph2d.kc —— **2D graphing mode**（`evaldraw.txt:1298` 那一档）。
//
//   omni run ext/evaldraw/examples/graph2d.kc
//   -> #gfx png .omni-cache/gfx/graph2d.png 320 240
//
// 主函数的形参表是 `(x,y)` ⇒ 宿主**每个像素调一次**，回值过默认那条调色板：
// `<= 0` 蓝、`>= 1` 红、中间是蓝→青→绿→黄→红那条光谱（见 `ext/polydraw/graph-rt.js`）。
// 网格是默认的 `setgrid(-4,3,4,-3)`，所以 x 从左到右是 -4..4、y 从上到下是 3..-3。
//
// 这一份刻意只用 x：**回值沿横向线性**从 0 走到 1 ⇒ 一条从左到右的光谱带，
// 每一行都一样。判据（`tests/lower`）按左中右三格像素核对，钉住"网格 + 调色板"这两件事。
(x,y)
{
   return x*.125+.5;
}
