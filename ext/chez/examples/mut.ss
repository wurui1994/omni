;; ext/chez/examples/mut.ss —— **改写一格已有的名字**（`set!`）
;;
;; 期望输出逐行相同：1 / 3。
;;
;; 单开一族的理由是**账上算出来的**：`set` 那格节点规格里十门都有，可矩阵里只量到九门 ——
;; 缺的正是 chez。而缺的原因不是机制（映射里 `case 'set!'` 一直都在），是**没有一份例子
;; 用它**：Scheme 那几份写的是纯递归（`(sum i acc)`），一处赋值都没有。
;; 于是这一份把那格判据补上 —— 账上"欠机制"与"欠判据"从此分得清。

(define n 1)
(display n)
(set! n (+ n 2))
(display n)
