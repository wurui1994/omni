' ext/freebasic/examples/conv.bas —— 与 go 那份 conv **同一件事**
'
' 期望输出逐行相同：2 / 3.5。
' FB 的转换是**一族有名字的函数**（`CInt` / `CDbl` / `Str` …），go 的是语法。
' 两种写法落同一格 `conv` —— 名字到目标的对照表在 FB 自己的映射里，不在节点里。
' （刻意用 7/3 而不是 7/2：`CInt` 是四舍五入、图上那格是截断，2.33 两家都给 2 ——
'   不拿"恰好都对"的例子当"语义一样"，这一条记在映射的不足里。）

Print CInt(7.0 / 3.0)
Print CDbl(7) / 2
