# 开发期的约定（见 docs/design/dev-deadline.md）
#
# 这一份是**开发期**的。发布那一档用 `--release` / OMNI_RELEASE=1，那时一格时限都没有。
# 两边各自读它：CLI 是 cli.js 的 dotEnv()，链出来的程序是 omni_js_host.c 的
# omni_dl_from_dotenv_()。`0` / `off` / `none` = 不限。

# 跑一个程序的墙上时限（秒）。链出来的可执行文件自己也读这一格。
OMNI_TIMEOUT=30

# 编译这一趟的墙上时限（秒）—— 编一份大东西要几分钟是正常的，所以与上面那格分开。
OMNI_BUILD_TIMEOUT=300
