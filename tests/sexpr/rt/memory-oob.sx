;; 线性内存的越界（ADR-0017 第二刀）。三份实现（interp/builtin.js 的 memChk、
;; backend-js 的 $lin_at、omni_linmem.c 的 omni_lin_at）必须冒出**同一句话**。
;;
;; 消息里印的是**字节地址 + 宽度 + 总字节数**，与指针那一路（按元素印）刻意不同：
;; 线性内存里"元素"这个概念不存在，地址就是字节地址，而且它在三套实现里是同一个数
;; （不像 arena 的裸地址）—— 所以印出来是有定义的。
;;
;; 一页 = 65536 字节，从 65530 读 8 个字节要读到 65538，越界。
(module
  (memory 1 1)
  (main
    (print (mload i64 (int 65530)))))
