;; 数组越界那句话（五条腿逐字节相同）。这一条是**后补的**，补的理由是一次改动：
;; JS 那条腿的 $aget/$aset 快路换成"undefined 当哨兵"（读回来是 undefined 就落慢路）之后，
;; 这句话只在**慢路**上才算出来 —— 从前它在快路上顺手就报了。没有这一条门，那次改动
;; 等于把这句话交给运气：五条腿里只有一条改了路，而消息各有一份实现。
(module
  (main
    (let a (arr int) (anew (arr int) (int 2)))
    (print (aget (var a) (int 5)))))
