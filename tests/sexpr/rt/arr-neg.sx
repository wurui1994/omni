;; 负下标走的是同一句话，但**写**那一侧（aset）是另一份实现 —— 读那条与写那条各有
;; 自己的快路与慢路，所以两侧各钉一条。负数这一格还顺手钉住"下标不当无符号读"：
;; 按无符号读的话 -1 会变成一个天文数字，印出来的就不是 -1 了。
(module
  (main
    (let a (arr int) (anew (arr int) (int 2)))
    (aset (var a) (int -1) (int 7))
    (print (aget (var a) (int 0)))))
