;; (rmath) 的白名单是**关着的**：名单是 C99 math.h 与 ECMA-262 Math 的**交集**，
;; 交集之外一律拒。这一层只转手宿主的数学库 —— `tgamma` 在 libm 里有，在 JS 的 Math 里
;; 没有，放进来就等于逼我们在这一层自己现编一份，那是不允许的（标准库有的东西不重造，
;; 标准库没有的东西也不在这一层补）。悄悄放进来会得到一份 JS 那条腿跑不了的方言。
(module
  (main
    (print (rmath "tgamma" (real 1.0)))))
