;; (rmath) 的白名单是**关着的**：认识的七个之外一律拒。理由不是懒 —— 是这条轴要求
;; 五条腿逐字节相同，而 libm 与 V8 在超越函数的最后一位就分叉（量过 atan/tan/log/cos）。
;; 悄悄放进来会得到一份"大多数输入都对"的实现，那比拒掉难查得多。
(module
  (main
    (print (rmath "sin" (real 1.0)))))
