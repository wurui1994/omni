;; **按值内嵌绕回自己**：`A` 里躺一个 `A`，那一格的大小是"自己加一点"，解不出来。
;; 第二十三刀之前这条报的是"直接内嵌的字段只能是前面已经声明过的结构体" —— 那条限制
;; 把"声明在后面的那个"也一起拒了，撤掉之后剩下的真毛病只有这一种，由 cutValueCycles 报。
;; 链表要的是**隔一层指针**（`(n (ptr A))`，第十七刀之后收了）—— 指针是三个字、与目标的
;; 布局无关，零值是空指针，没有递归。这条钉的是"少写了那一层指针"的那一半。
(module
  (struct A (n A) (x int))
  (main
    (let a A (new A))
    (print (fld (var a) x))))
