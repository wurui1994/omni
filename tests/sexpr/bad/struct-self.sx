;; **直接内嵌**的字段类型不能是自己：零值要在构造的时候铺满，自引用就是无限递归。
;; 链表要的是**隔一层指针**（`(n (ptr A))`，第十七刀之后收了）—— 指针是三个字、与目标的
;; 布局无关，零值是空指针，没有递归。这条钉的是"少写了那一层指针"的那一半。
(module
  (struct A (n A) (x int))
  (main
    (let a A (new A))
    (print (fld (var a) x))))
