;; 不插隐式装箱（与这一层"类型不推导，只检查"同一条）：要 dyn 就得写明 `(dyn E)`。
;; 这一格钉住的是**那条边界本身** —— 一格 int 直接坐进 dyn 的位置上要当场报，
;; 而不是悄悄替谁装一次箱（"什么能悄悄转成什么"是门语言设计决定，汇聚层不替谁定）。
(module
  (main
    (let d dyn (int 42))
    (print (dtag (var d)))))
