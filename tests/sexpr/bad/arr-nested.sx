;; 数组套数组还不收。**理由这一刀换了**（第十八刀之前是"MIR 那层只有一个 8 位类型码"）：
;; 身份已经有了 —— 数组指令的 aux 现在是**数组类型号**，元素的完整类型挂在类型池的 oir 上。
;; 剩下的是**语义**那一半：`(anew T N)` 的零值是一个**求过一次**的操作数，运行时把那一份
;; 复制 N 遍。元素是引用（类、数组）时"复制"就是拷句柄 —— 类元素要的正是这个（空引用铺满），
;; 但多维数组要的是 N 条**各自独立**的行，那要"每格造一个新的"，而五条腿的 anew 都没有
;; 这个形状。收进来就是 `a[0]` 上 push 一格之后 `a[1]` 也长了一格 —— 一句静默的错答案。
;; 向量元素能收是因为它是值语义：复制那 16 字节正是要的东西。
(module
  (main
    (let a (arr (arr int)) (anew (arr (arr int)) (int 1)))
    (print (alen (var a)))))
