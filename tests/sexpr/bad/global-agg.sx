;; 模块级变量这一刀（第二十四刀）只收标量。聚合的**身份**不在 MIR 的 8 位类型码里
;; （`(arr int)` 与 `(arr string)` 在那里是同一个码，身份在类型池的 aux 上），
;; 而全局池现在只有「名字 + 类型码」这一列，所以带身份的全局要先给那张池子加一列。
;; 那是另一刀 —— 不猜一个半通的实现，先把边界划在这里。
(module
  (class Node (v int))
  (global here Node)
  (main
    (print (int 1))))
