;; 结构体的字段这一刀只收标量。不是"少写几行"：每条腿的**结构体零值**都是各自一个小
;; 函数（JS 后端的 zero、C 后端的 zeroExpr、解释器的 zeroOf），它们今天只认标量；
;; 数组字段还要先定"复制结构体时复制的是句柄还是内容"。那是下一刀，这里先钉住边界。
(module
  (struct Bag (xs (arr int)))
  (main
    (let b Bag (new Bag))
    (print (alen (fld (var b) xs)))))
