;; 数组字段还在门外 —— 向量字段第十五刀已经进来了（见 cases/08-vecfields.sx），所以
;; 这条边界剩下的理由只有一个：**零值的形状**。向量的零是一个常量（LLVM 那边就是
;; zeroinitializer），而数组的零是一次运行时调用（omni_arr_*_new(0, 零值)），而这条腿的
;; NEW 现在只会 `store 一个常量`。还要先定"复制结构体时数组字段拷的是句柄还是内容"。
;; 那是下一刀，这里先钉住边界。
(module
  (struct Bag (xs (arr int)))
  (main
    (let b Bag (new Bag))
    (print (alen (fld (var b) xs)))))
