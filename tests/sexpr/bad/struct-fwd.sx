;; **直接内嵌**的字段可以是别的结构体，但只能是前面已经声明过的那些：一遍收记录。
;; "两遍收记录"要先答"互相嵌套的零值怎么铺"，那不是这一刀的事。
;; 隔一层指针（`(a (ptr Point))`）不受这一条约束 —— 见 struct-self.sx 那一段。
(module
  (struct Seg (a Point) (b Point))
  (struct Point (x real) (y real))
  (main
    (let s Seg (new Seg))
    (print (fld (fld (var s) a) x))))
