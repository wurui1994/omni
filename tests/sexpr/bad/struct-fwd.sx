;; 字段的类型可以是**别的**结构体，但只能是前面已经声明过的那些：一遍收记录。
;; "两遍收记录"要先答"互相嵌套的零值怎么铺"，那不是这一刀的事。
(module
  (struct Seg (a Point) (b Point))
  (struct Point (x real) (y real))
  (main
    (let s Seg (new Seg))
    (print (fld (fld (var s) a) x))))
