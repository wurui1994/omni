;; (tostr E N) 的 N 是**格式的一部分**，不是运行期才知道的东西：写成变量就报错。
;; 这条边界是刻意的 —— 位数一旦能变，C 那条腿就得自己拼格式串，两条 JS 腿也没法
;; 在生成期把它折进来，而"四份实现给出同一串字符"就没有构造性了。
(module
  (main
    (let p int (int 15))
    (print (tostr (real 0.5) (var p)))))
