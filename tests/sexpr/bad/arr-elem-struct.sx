;; 数组的元素是**结构体**（值语义）还不收 —— 类（引用语义）这一刀收了，两者的差别就在
;; "存进去要不要拷"：
;;   - 类元素：格子里躺的是句柄，`aset`/`apush` 存指针，五条腿一句拷都不用发；
;;   - 结构体元素：格子里躺的是**内容**，于是 anew（零值铺满 N 格）、aset、apush 三处都要
;;     按元素类型拷一份。C 与 LLVM 那两条腿白捡（前者是结构体赋值、后者是按字节的 blob），
;;     但 JS 与解释器那两条腿的 `arrCopy` 是**类型擦除**的 —— 它只认 Array.isArray，
;;     一个普通对象原样返回，于是三个格子会共用同一个对象。
;; 放开的正确做法是让那两条腿在数组的读写点也走 copyOf（跟 `$cp_S` 那条一样），
;; 那是另一刀；在此之前拒掉，而不是让两条腿静静地给出不同答案。
(module
  (struct Point (x real) (y real))
  (main
    (let a (arr Point) (anew (arr Point) (int 2)))
    (print (alen (var a)))))
