;; 整块**写**同理（第十八刀）：`(blk T N)` 不是一个值，右边不可能有一个它那型的表达式。
;; 这一条钉的是诊断的**指向** —— 报的是"指针要先退成元素指针"，而不是"换个值"。
(module
  (main
    (let a (ptr (blk int 4)) (pnew (ptr (blk int 4)) (int 2)))
    (pstore (var a) (int 1))))
