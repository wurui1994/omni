;; `(fail E)` 的实参必须是 string。方言里这一条是给**被降级的语言**用的：它们有自己的
;; 运行期错误（asy 的 angle((0,0)) 就是量出来照搬的），不是"还没做"。
;; 拒非 string 的理由与 print 那几条同源：运行库那个符号收的是 omni_str，
;; 让这一层替谁做一次隐式转换，就等于替六条腿各定一遍格式。
(module
  (main
    (fail (int 7))))
