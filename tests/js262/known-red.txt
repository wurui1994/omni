# 还没实现的用例，一行一个（ADR-0020 的 P1..P6 一格一格拆）。
# 红着不算门失败；**绿了会报出来** —— 那是"把这一行删掉"的信号。
#
# 2026-09-08 的基线：12 个全红。往下走的时候一行一行删。
# 已经绿的（删掉的）：obj-accessor（字面量访问器 + 真 this）、obj-freeze（freeze/seal 那一族）。
# 已经绿的（删掉的）：obj-accessor、obj-freeze、obj-key-order、for-in、to-primitive。
# 已经绿的（删掉的）：obj-defineproperty、symbol-basic —— 两条都是被 js_obj_own_keys 的
#   lit 字段名（原先叫 kind，撞上 IR 节点的判别字段）压住的，改名 sel 之后一起绿了。
# 已经绿的（删掉的）：proto-chain、class-static-field —— 字面量里的 __proto__ 与
#   Object.prototype 上那几个名字（hasOwnProperty/valueOf…）通了之后绿的。
# 已经绿的（删掉的）：iter-protocol —— 后缀自增能在惰性位置（三元分支）里降了之后绿的。
# 已经绿的（删掉的）：this-call-apply-bind —— Array.prototype 上摆了借得最多的那几个方法
#   （数组的方法平时按标签派发，原型上本来一个都没有）。
# 已经绿的（删掉的）：class-extends —— "右边是内建构造器的名字"那种 instanceof 走
#   js_instanceof_p（拿 realm 上那一格 prototype 比原型链）之后绿的。
#
# 现在**一条不剩**。往后新增的用例一时红着，就在这儿记一行，并写清欠的是哪一格。
