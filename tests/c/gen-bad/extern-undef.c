/* `extern` 声明了但整个翻译单元里没有定义，而代码用了它。真的编译器要等链接期才知道
 * 这件事；我们只有一个翻译单元，所以「用过但没定义」当场就是错。
 * 这条钉的不是「还没做到」，是一条**真的诊断** —— 做到链接期符号那一片时它要么保持红，
 * 要么换成「链接时找不到」，反正不能变绿。 */
extern int nowhere;

int main(void) {
  return nowhere;
}
