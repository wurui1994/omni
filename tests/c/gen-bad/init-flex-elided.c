/* 不定长数组配省掉里层花括号：长度这一遍是在**记号层面**数的（`sizeFromInit`），
 * 而省了花括号之后「一格」不再等于「一个记号」—— 得先按元素类型分格才数得出 2。
 * 那是 `initBraced` 那个下降栈的活，两遍合一遍还没做，所以当场报错。 */
int main(void) {
  int a[][2] = { 1, 2, 3, 4 };
  return a[1][1];
}
