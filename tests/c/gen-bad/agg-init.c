/* 嵌套数组省掉里层花括号还没做到（第 8 片做了写全花括号的那些）。
 * C 允许 `int a[2][2] = {1,2,3,4}`：要支持它，`initArray` 得把「这一层还没吃完的
 * 元素」交给下一层去接着吃，那是一套跨层的游标。写全花括号的那条路不需要它。 */
int main(void) {
  int a[2][2] = { 1, 2, 3, 4 };
  return a[1][1];
}
