// `(sgen E N)` 的位数越界（第三十一刀）。与 `sfix` / `ssci` 同一条：范围 0..30，字面量
// 当场判、一段表达式落到运行期，四份实现（$str_gen、omni_str_gen、两个解释器共用的
// fmtGen 那一处调用点）**同一句话**。
//
// 带 `#` 的那一支是另一个名字，所以另一句话（见 rt/sgenk-range）—— 诊断要指得准。
int main() {
    int n = 40;
    printf("%.*g\n", n, 1.5);
    return 0;
}
