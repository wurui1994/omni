// `(sfix E N)` 的位数越界（第二十八刀）。方言给它的范围是 0..30 —— 上界让 C 那侧的
// `%.*f` 缓冲有个头。位数是字面量时那一条当场判（降级期报错），可第二十八刀让它**也能是
// 一段表达式**，那时就只能落到运行期：四份实现（JS 后端的 $str_fixed、C 运行时的
// omni_str_fixed、两个解释器共用的 fmtFixed 那一处调用点）各自查，**同一句话**。
//
// 这一份量的正是"同一句话"：五条腿都得报 `sfix precision out of range: 40 (0..30)`。
// 悄悄夹到 30 也是一种做法（C 运行时以前就是那样兜底的），但那会给一个**错的答案** ——
// C 那边 `%.40f` 是真印 40 位的，夹一下就不是 jancy 的答案了。宁可当场停。
int main() {
    int n = 40;
    printf("%.*f\n", n, 1.5);
    return 0;
}
