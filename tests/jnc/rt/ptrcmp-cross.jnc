// 跨块比大小（第四十六刀）。C 那边这是**未定义行为**（C99 6.5.8p5：只有指向同一个数组
// 对象的指针之间才有次序），jancy 那边是"都转成 intptr 再比"，于是答案取决于两块内存在
// 地址空间里的先后 —— 那在 arena 与真指针两套实现下不是同一个数。
//
// 这一层降成 `(psub p q) < 0`，于是这件事落在 psub 的那条检查上：跨块相减是运行期错误
// （sexpr/lower.js:1750）。五条腿上报的是同一句 —— 把 UB 换成一句看得见的错。
int main() {
	int* p = new int[3];
	int* q = new int[3];
	printf("cmp=%d\n", p < q);
	return 0;
}
