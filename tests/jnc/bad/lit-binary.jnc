// 二进制字面量（第五十四刀的边界）。`0x"61 62 63"` 定义的是一块**逐字节**的 const char
// （literals.rst:33-44：hex / oct / bin / dec 四种进制都行，空格随便分组），而它常见的用处
// 正是"去掉 C 字面量末尾那个零"（同一份 :46-50 的 `char d[] = "non-zero-terminated" 0x"";`）。
//
// 这一层收不了它，卡的不是语法而是**存储宽度**：方言里一格整数占 64 位（ADR-0016 决策二那处
// 刻意留下的差别），所以"一块 3 个字节的 char 数组"落下来会是 3 格 8 字节。要它得先让方言的
// 布局认字节宽度 —— 与 `sizeof` 是同一格（那一条记在 bad/sizeof.jnc）。
//
// 拼接（第五十四刀）只折 C 字面量那一种：`"a" 0x"61"` 里的第二段折不动，所以整条明说不收。
int main() {
	printf("%s\n", 0x"61 62 63");
	return 0;
}
