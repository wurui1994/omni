// 宿主面同名那几条**能挑了**（第一百七十一刀）：按个数筛、按实参类型排，符号名头一条仍叫
// `Owner_method`、之后的叫 `Owner_method_o2` / `_o3`…（那是我们自己的约定 —— jancy 那边重载
// 是宿主一条条 `JNC_MAP_OVERLOAD` 登记、名字由写绑定的人定，没有可照抄的推导规则）。
//
// 这一条留下来钉的是**同元分不出来**那一格：`1` 是整数字面量，而 jancy 对常量的 int -> int
// 加宽算 `Identity`（jnc_ct_CastOp_Int.h:23-111），所以 `lock(int)` 与 `lock(long)` 两条一样
// 合得上 —— 那在它那边也是 "ambiguous call to overloaded function"。这一层于是不给答案。
// （第一百五十五刀那次量出来的同一格：`construct(int)` / `construct(double)` 不是平手，因为
// int -> real 严格更差；要造平手得两条都是整数。）
opaque class Lock {
	int m_depth;

	void lock(int n);
	void lock(long n);
}

int main() {
	Lock* p = new Lock();
	p.lock(1);
	return 0;
}
