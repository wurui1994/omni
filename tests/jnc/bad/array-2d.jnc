// 多维数组。`int a[10][20]` 是 decl_simple.rst 里那个例子本身（"This declares a
// two-dimensional array of integers (10 rows and 20 columns)"），所以它属于「借来的
// 完整语法」的一部分 —— 这里拒的理由不是"jancy 没有"，是**方言的指针指不到数组**：
// `ptrTargetOk` 只放行 int / real / bool / struct，而 `int[10][20]` 的元素是 `int[20]`。
// 与 `&p`（指针的指针）、整个结构体的 pload/pstore 是同一格：要方言先能把多个字的值
// 当内存里的东西搬。
int main() {
    int a[10][20];
    a[0][0] = 1;
    printf("%d\n", a[0][0]);
    return 0;
}
