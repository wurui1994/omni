// 第一百四十五刀的界：一格局部声明的**初值**降不下来时，那个名字仍旧进作用域。
//
// 这一份要钉住的是"第二句报的是什么"。第一句的初值类型对不上，报一条；先前那时候连名字都不记，
// 于是第二句跟着报"未声明的变量 'p'"—— 一句错长出两句，而后面那句指着的是**别处**。
// 声明的类型是写出来的，初值算不出来一点也不影响它，所以记下来不是猜。
//
// 于是第二句报的该是它自己那件事：那个结构体里没有这个字段。这一份的期望值就是那一句 ——
// 把这一刀撤掉，这儿出现的会是"未声明的变量 'p'"，对不上。
//
// 语料里的原样：`GroupProperty* prop = new GroupProperty(name);`（ui_PropertyGrid.jnc:420，
// 那个类的 construct 只有原型）之后每一句用到 prop 的都报"未声明的变量"——
// 逐份那张榜上 296 处、一起编那张榜上 45 处，全是这么长出来的。

struct S {
	int m_a;
}

int main() {
	S* p = 1;
	p.nosuch = 2;
	return 0;
}
