// 同元重载，而实参的类型**不降一遍问不出来** —— 这一层不收（第八十刀划的那条界）。
//
// 挑哪一条要先知道实参是什么类型（jancy 那边是 chooseOverload 的那个标量，
// jnc_ct_FunctionTypeOverload.cpp:44-91）。可这一层的 `expr` 是**按 want 定向**的：
// `null` 要 want 才知道是哪种指针、花括号初值要 want、整数字面量的宽度也看 want；而且降的
// 时候会**发码**（errorcode 的传播提升、提临时量）。所以不能"先降一遍拿类型、再按选中的那一条
// 降第二遍" —— 那会把同一段代码发两遍。
//
// 于是这一层只对"不降也知道类型"的实参排序：字面量、`true`/`false`、以及查得着的名字
// （局部量 / 形参 / 模块级 / 方法体里裸写的字段 —— 那三个查名都是纯查表）。别的当场说清。
// 这里那个实参是**一次调用**（`mk()`），要它的类型就得先把它降出来。
//
// 要收下来得先给 expr 加一格"只问类型、不发码"的干跑模式，那是自己一刀（任务 #31 的 (a)）。
int mk() {
	return 1;
}

void p(int v) {
	printf("p int %d\n", v);
}

void p(double v) {
	printf("p double %.1f\n", v);
}

int main() {
	p(mk());
	return 0;
}
