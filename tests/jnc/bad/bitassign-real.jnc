// `&= |= ^= <<= >>=` 只在整数上有定义（C 6.5.16.2、jancy 同）。`double` 上写它当场拒 ——
// 这不是"还没长出来"，是**没有可对的答案**：位运算要的是整数那串位，浮点那串位不是它。
//
// 第三十五刀之前这一行也拒，但拒的理由是"复合赋值 '&=' 还没接"；现在理由换成了类型。
int main() {
    double x = 3.5;
    x &= 1;
    printf("%f\n", x);
    return 0;
}
