// 格式化字面量里剩下的裸 `%` 撞上 printf（第六十四刀留下的边界之二）。
//
// jancy 的词法里只有配得上 `lit_fmt_spec` / `lit_fmt_index` 的那几种才算注入
//（Lexer.rl:131-133），配不上的 `%` 原样留在字面量的数据里 —— `100%` 后面跟的是 `\`，
// 转换字符那一格配不上，所以这个 `%` 就是一个字符。
//
// 于是 jancy 那边这一句要**解释两遍**：字面量先产出一格 char*，printf 再把里头那个 `%`
// 当格式说明读一次。这一层的 printf 是编译期展开的（格式串必须是字面量），没有运行期的
// 格式解释，所以这一格明说不收，而不是悄悄少印一个 `%`。
//
// 拦的只是"再喂给 printf"这一步：同一个字面量落在**字面量位置**上是收的（结果就是那几个
// 字符，见 cases/61-fmtlit.jnc 的最后两行）。
int main() {
	printf($"done: 100%\n");
	return 0;
}
