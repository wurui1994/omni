// 类的成员属性（ADR-0016 第六十八刀的边界）。
// 第六十八刀只做**顶层**那一格。成员属性还要三样：`obj.p` 的读挂在取字段那一处、写挂在
// lvalue 那一处、方法体里裸写属性名要补 `this`（与第五十二刀给方法补 this 是同一条）。
// 那三处各自都有别的分支要让路，是自己一刀。
class Box {
	int property m_value;
}

int main() {
	Box* b = new Box();
	return 0;
}
