// 第八十刀那条界剩下的那一半（第一百四十四刀把它缩小到这儿）：同元重载，而实参的类型
// **不降一遍问不出来**。
//
// 那条界本身没动：挑哪一条要先知道实参是什么类型（jancy 那边是 chooseOverload，
// jnc_ct_FunctionTypeOverload.cpp:44-91），可这一层的 `expr` 是**按 want 定向**的（`null` 要
// want 才知道是哪种指针、花括号初值要 want、整数字面量的宽度也看 want），而且降的时候会
// **发码**（errorcode 的传播提升、提临时量）。所以不能"先降一遍拿类型、再按选中的那一条降
// 第二遍" —— 那会把同一段代码发两遍。
//
// 变的是**哪些实参算"不降也知道"**：第一百四十四刀之前只有字面量、`true`/`false` 与查得着
// 的名字，之后又添了 `new C(…)`、字段、非重载的调用、`&x`、枚举成员五种（见
// cases/135-overloadcheap.jnc）。这里剩下的是**一次二元运算** —— 它的类型要按两边提升出来，
// 而两边各自又可能要降，所以这一格仍旧只能说清。
//
// 要收下来得先给 expr 加一格"只问类型、不发码"的干跑模式，那是自己一刀（任务 #31 的 (a)）。

void q(int v) {
	printf("int %d\n", v);
}

void q(double v) {
	printf("double %.1f\n", v);
}

int main() {
	int x = 1;
	q(x + 1);
	return 0;
}
