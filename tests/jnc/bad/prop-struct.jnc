// 结构体的成员属性（ADR-0016 第六十九刀的边界）。
// 属性的取/存那两个函数是从**类那一层**查过来的：写在类外的 `int C.p.get()` 与写在体里的
// `int p.get()` 靠的是同一件事 —— 类是一层命名空间（第五十二刀）。结构体在这一层不是，
// 所以那两个体没有落脚处。jancy 那边结构体也能有属性（属性不进结构体的内存，与字段不同格），
// 要接就得先给结构体一层命名空间 —— 那是另一刀。
struct Rect {
	int m_w;
	int property m_area;
}

int main() {
	Rect r;
	return 0;
}
