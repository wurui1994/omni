// 两格无名枚举漏出同一个名字（ADR-0016 第九十六刀的边界）。
// jancy 那边无名枚举的成员是 `addItem` 进外面那层命名空间的，重名当场就是一句错；
// 这一层同理 —— 两格都想在外面占 `Same` 这个名字，谁赢都不对，所以拦着。
enum {
	Same = 1,
}

enum {
	Same = 2,
}

int main() {
	printf("%d\n", Same);
	return 0;
}
