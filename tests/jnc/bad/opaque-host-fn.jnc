// `opaque class` 上的方法**能降**（ADR-0022 的 J4b）：`p.lock(…)` 是一次对 `Lock_lock(self, …)`
// 的真调用，正面判据在 `tests/llvm/run.js` 第 9 节（那儿有宿主的体）。
//
// 这一条钉的是**边界**：签名要落进 C_ABI 那几个词。两条老账这一轮都兑了 —— 枚举按底整数过
// （第一百六十三刀），`variant_t` 按一格地址过（第一百七十三刀）。剩下的这一格是**函数值**：
// jancy 的 `function*` 是**胖的**（一个函数地址加一格闭包，ADR-0010:84-85 记的那笔），而这一层的
// 函数值是**一个**指针。过去要么把两个字都过（那得先定"哪个字在前"这条约定），要么明说不收。
// 按一个字糊过去是猜 —— 宿主那边闭包那一格会读到别的东西，而错的调用约定在运行期是读错地方、
// 不报错。
opaque class Lock {
	int m_depth;

	void onEach(void function* f(int));
}

void tick(int n) {
	printf("%d\n", n);
}

int main() {
	Lock* p = new Lock();
	p.onEach(tick);
	return 0;
}
