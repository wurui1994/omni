// `opaque class` 上的方法**现在能降了**（ADR-0022 的 J4b 最后一步）：`p.lock()` 是一次对
// `Lock_lock(self, …)` 的真调用，正面判据在 `tests/llvm/run.js` 第 9 节（那儿有宿主的体）。
//
// 这一条留下来钉的是**边界**：签名要落进 C_ABI 那几个词。枚举落不进 —— 它在这一侧是一格
// 有自己规范形的类型（第三十九刀），而那一端只有 i32/i64/f64/bool/ptr/void 可写。
// 悄悄按底整数传过去是**猜**，而猜错的调用约定比报一句错难查得多。
enum Mode { fast, slow }

opaque class Lock {
	int m_depth;

	void lock(Mode m);
}

int main() {
	Lock* p = new Lock();
	p.lock(Mode.fast);
	return 0;
}
