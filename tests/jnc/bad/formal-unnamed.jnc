// 无名形参 —— 这一层还不收（这条界原来藏在"函数类型的 typedef"后面，第八十二刀把前者
// 收了之后它才露出来）。
//
// jancy 收：形参的名字是可选的（Declarator.llk 那条 `declarator?`），语料里的原型也常这么写
// （`typedef void stdcall FooFunc(int);`）。这一层的形参名字是要用的 —— 每个形参在方言里
// 都是一格 `(名字 类型)`，名字从声明符上抄；没有名字就得自己编一个，而编出来的名字要保证
// 不撞上用户写的别的名字，那是自己一格（与 cellName 那一套同一个问题）。
//
// 拒在这儿而不是悄悄编一个：编了之后这条声明就有了一格**源码里不存在的名字**，往后
// "按名字查形参"的地方（默认值、重载的诊断）都会拿它去比，那是个静默的错。
typedef int F(int);

int main() {
	printf("%d\n", 0);
	return 0;
}
