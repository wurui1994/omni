// 完整声明式属性的体里放一格**不带 `autoget` 的普通字段**（第二百六十一刀立的墙）。
//
// 这一格是 ADR-0029 那张位置矩阵撞出来的（`property-body × field-*`），而**不是**语料撞的：
// 语料榜量的是诊断行，而这一格先前整个前端**抛 TypeError**（`lower.js` 的 flatMods 读
// `lst.span`，可 `int m_i;` 那种一个修饰词都没写的说明符只有三项，`items[3]` 压根不在）——
// 一条诊断都没有，榜自然看不见。矩阵上那 9 格 `炸` 全是这同一处。
//
// 这堵墙钉的是"**不许再炸**"：话还不够准（第一百七十九刀那段注写着 jancy 收这一条，
// 类型从 get 抄），可"抛异常"与"说不收"是两回事 —— 前者是我们自己的 bug。
// 真收下它是另一刀，账记在 ADR-0029 的矩阵里（那一列还有 10 格 E）。
int property g_pp {
	get {
		return 1;
	}

	int m_i;
}

int main() {
	printf("%d\n", g_pp);
	return 0;
}
