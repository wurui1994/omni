// 完整声明式的属性里**在体内写字段**（第七十五刀留的账）。jancy 那半是
// 「'autoget' field implicitly makes property 'autoget'」（prop_full.rst:34，
// samples/jnc/34_BindableProperties.jnc:46-59 就是这个形状）：体里那格字段与
// `bindable event` 会反过来给整格属性加上 autoget / bindable 两个性质。
// 这一层收的是"体里只有带体的 get / set"那一种（cases/72-propfull.jnc），字段这一半还没落 ——
// 不拦就会把那格字段悄悄丢掉。
property g_p {
	autoget int m_x;

	void set(int x) {
		m_x = x;
	}
}

int main() {
	g_p = 1;
	printf("%d\n", g_p);
	return 0;
}
