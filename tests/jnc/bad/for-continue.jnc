// 带步进的 `for` 里的 `continue`（第四十一刀之后剩下的那条边界）。
//
// 方言里没有 `for`：这一层把它摊成 `while`，步进搬到体的**末尾**。而方言的 `cont` 跳到
// 循环头 —— 直接接就会漏掉一次步进，`i` 永远不动，成死循环。与其给个错答案，不如在这儿停下。
//
// 落地要给 for 的体套一圈**一次性**循环（`(while (bool true) (do <体> (brk)))`），让 `continue`
// 变成那圈的 `(brk)`，落点正好在步进之前。多层的 `(brk N)` 已经有了（第四十刀），所以这一格
// 现在只差"什么时候套"这一个判断 —— 那是下一刀。
//
// 这一条替掉了原来的 bad/switch-continue：switch 里的 continue 第四十一刀已经落地
// （cases/39-breakn.jnc 的 f3），那条边界删了。
int main() {
    int acc = 0;
    for (int i = 0; i < 5; i++) {
        if (i == 2)
            continue;
        acc += i;
    }
    printf("%d\n", acc);
    return 0;
}
