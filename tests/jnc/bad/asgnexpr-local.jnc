// 第一百一十四刀的界：表达式里赋值，**左边要是一格内存**。SSA 里的局部量没有地址 ——
// 那一格助手（`jnc$asgn$T(p, x)`）第一个实参要的正是地址，写不进去。
// 语料里 14 处逐处看过，左边全是字段路径或指针，一处都不是 SSA 局部量，所以这条界不挡任何
// 真写法。要收它得让"赋值那一句 + 把左边当值"两件事在同一个表达式里排上顺序，那是另一刀。
int main() {
	int x = 0;
	int y = (x = 5);
	printf("%d %d\n", x, y);
	return 0;
}
