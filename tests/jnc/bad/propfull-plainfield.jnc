// 完整声明式的属性体里写**不带 `autoget`** 的字段（第七十六刀的边界）。
// jancy 那边属性体是一层命名空间（prop_full.rst:15），里头可以放任何声明；而这一层只认两种：
// `autoget` 的那格存储（prop_full.rst:34）与 `bindable event` 的那格事件。别的落进来就会被
// 悄悄丢掉 —— 那格字段没人发、体里读它就成了"未声明的变量"，所以当场拒。
property g_p {
	int m_x;

	int get() {
		return m_x;
	}

	void set(int x) {
		m_x = x;
	}
}

int main() {
	g_p = 1;
	printf("%d\n", g_p);
	return 0;
}
