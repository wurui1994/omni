// 串字面量**放不下**那一格数组（第二百六十二刀的界）：`char a[2] = "abc"` —— 3 个字符还要
// 一格零尾，一共要 4 格。这是**源码的错**（jancy 那儿同：串比数组长时那一句转换就不成立），
// 不是"我们还没接"—— 那一族的正面判据在 cases/204-strarray.jnc。
int main() {
	char a[2] = "abc";
	printf("%d\n", a[0]);
	return 0;
}
