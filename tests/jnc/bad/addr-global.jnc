// 对 **string** 的模块级变量取地址。第二十四刀把标量的模块级变量做成了可寻址的
// （被 `&` 取过地址的那格发成 `(ptr T)`，值躺在一段 pnew 出来的内存里），但 string 不在
// liftable 那张名单上 —— 方言的 `(ptr T)` 目标只收落得进内存的那些（hir/types.js 的
// ptrTargetOk），字符串在这一层是一个不透明的引用，没有"一格字节"可指。
//
// jancy 那边 `&gs` 是合法的（它的 string 是带长度的 `char const*` 那一族，有真的表示）。
// 所以这一条仍旧是**方言这一侧**的边界，不是 jancy 的：要接就得先答"字符串在内存里长什么样"。
// 局部量那一半是同一条（liftable），报的是同一句话。
string_t gs = "hi";

int main() {
    string_t* p = &gs;
    printf("%s\n", *p);
    return 0;
}
