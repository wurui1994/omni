// 对模块级变量取地址。第九刀把**局部量**做成了可寻址的（照 jancy 的办法提到 GC 堆上），
// 但模块级变量在方言里是一格全局 —— 全局不在一段可寻址的内存里，`(pnew …)` 那条路不适用。
//
// jancy 那边 `&g` 是合法的（它的全局本来就有地址）。所以这一条是**方言这一侧**的边界，
// 不是 jancy 的：要接就得让方言的全局也落在一段内存里（也就是"全局也走 pnew"），那会
// 动到 MIR 的全局号与四条腿上全局的发法，单独一刀。
int g = 5;

int main() {
    int* p = &g;
    printf("%d\n", *p);
    return 0;
}
