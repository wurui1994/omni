// 第一百一十二刀的界（五）：**类**里的位域还不收。这一层的一整条继承链共用**一格**结构体
// （第五十六刀），而位偏移是按"本类体内的顺序"算出来的 —— 那两件事要一起接才对。
// 语料里类上一格位域都没有（13 份带位域的文件全是 struct），所以这一格留着当账记。
class C {
	uint8_t m_a : 4;
}

int main() {
	C c;
	c.m_a = 5;
	printf("%d\n", c.m_a);
	return 0;
}
