// 原型与定义**只有返回类型不同**（ADR-0016 第九十二刀钉的界，第二百〇八刀把话说准）。
// jancy 那边这一格是错的（"conflicting return types"）：重载只按**形参**分、返回类型不参与，
// 所以形参一模一样而返回类型不同的两条不是两个函数，是同一个函数写了两遍、且写岔了。
// 悄悄按其中一份算是骗人：调用点按原型检查、体按定义编，两边就错开了。
//
// 形参**不一样**的那一种不在这条里 —— 那是同名那一族里的另一条（jancy 收它），
// 墙在 bad/proto-hostovl.jnc，拦的理由是这一层的另一件事（方言里两边撞名）。
int twice(int x);

int main() {
	printf("%d\n", twice(3));
	return 0;
}

double twice(int x) {
	return x * 2.5;
}
