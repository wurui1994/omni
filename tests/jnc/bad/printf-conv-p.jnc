// printf 的**转换字符**那一族里最后一条：`%p`（第三十二刀把 `%u` 收了）。榜上"printf 的转换
// '…'"那一行**全是它** —— 全语料量到 59 处、逐份榜上 15 对，于是第二百三十八刀把下面这段
// 早就想清楚了的话搬进了诊断本身：先前那句只报个转换符的名字，把这条**刻意**的界说成了像
// "还没做"。
//
// 它与前面那几条**不是同一种拒**：`%e` / `%g` / `%u` 都是"还没长出来"，做掉就落地了；
// `%p` 是**这一层刻意不做**。理由在决策一里：五条腿上指针是**两套实现** —— run / interp /
// interp --mir 三条是 arena 模拟（地址是块内偏移），run-c / run-llvm 两条是真指针。
// 裸地址在这两边根本不是同一个数，所以"印出来"这件事没有一个五条腿都对的答案。
// 而这条纪律（不许观测裸地址）正是那两套实现能一直对得上的原因 —— 为了 `%p` 破掉它，
// 换来的是整条轴失去意义。
//
// 想看指针的话，这一层给的是能观测的那些：`p == null`、`*p`、`p[i]`、`p - q`（元素个数）。
//
// 到这儿 printf 的表面就齐了：五个标志、宽度、精度、`*` / `.*`、以及
// `d i u o x X e E f g G c s %` 这些转换。剩下的是**长度修饰符**（`%ld` / `%llu` / `%hhd`
// 那一族）—— 这一层的位数从**类型**上取，所以 C 那边要写修饰符的地方这边不用写；
// 真遇到 jancy 源码里写了修饰符，那是下一条边界，不是这一条。
int main() {
    int x = 1;
    int* p = &x;
    printf("%p\n", p);
    return 0;
}
