// 没写初值的函数指针（第五十五刀的边界）。jancy 保证每一格出来都是零，函数指针的零就是
// "空的函数指针"，调它是运行期的错。
//
// 这一层卡的是方言：函数值那一格**没有空值** —— `(fnty …)` 上没有 `pnull` 那样的东西，而
// `(let …)` 要一个初值。所以 `p` 那一格发不出来。跟着立不住的还有 `if (p)`（拿函数指针
// 当条件问"它是不是空的"）与 `p == null`。
//
// 模块级的那一格不在这条里：`(global g (fnty …))` 方言收，只是赋过值之前调它是未定义的
// —— 与空的类引用同级（见 cases/52-fnptr.jnc 里的 g_op）。
int add(int a, int b) {
	return a + b;
}

int main() {
	int function* p(int, int);
	p = add;
	printf("%d\n", p(1, 2));
	return 0;
}
