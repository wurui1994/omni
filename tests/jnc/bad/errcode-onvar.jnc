// 第一百四十九刀留下的那条界：`errorcode` 写在一格**变量**上。
//
// 那个词说的是"这个函数的返回值就是错误码"（exceptions.rst:17），所以它只对函数有意思。
// 变量上写它没有可对的意思 —— 收下不看就是把一个词悄悄丢掉。
//
// 第一百四十九刀动的是**问这一句的位置**：先前顶层那一遍一律按"不是函数位置"调 specs，于是
// `size_t errorcode transmit(void const* p, size_t size);`（ias.jnc:30 —— 一条**函数原型**）
// 也被这句话拦住，那是认错人。现在顶层收下那几个词往上传，再按"这条声明符到底有没有形参表"
// 分开说：有形参表的是函数（那几个词写得对），没有的才落到这一句上 —— 也就是这一份。
//
// 同一条支线上 `virtual` / `override` / `abstract` 那三个词也一样（它们只对类的方法有意思）。

int errorcode g_code;

int main() {
	printf("%d\n", g_code);
	return 0;
}
