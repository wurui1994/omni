// 第一百二十五刀的界：结构体拿**类**当基类。类是引用语义、还带一格动态标签（第五十七刀的
// 虚派发按它选），把它摊进一格按值的结构体里要先定"那个标签归谁、拷一份的时候怎么算" ——
// 那是单独一笔账。反过来（类拿结构体当基类）也还没收，落在另一句话上。
class C {
	int m_c;
}

struct S: C {
	int m_s;
}

int main() {
	S s;
	s.m_s = 1;
	printf("%d\n", s.m_s);
	return 0;
}
