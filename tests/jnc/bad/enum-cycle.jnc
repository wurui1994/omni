// 第一百四十一刀的界：枚举成员的初值**引用成环**。
// A 的初值要 B、B 的初值要 A —— 重试那几轮里一格都解不开，那时就该报出来。
// 报两句：先是"枚举 'B' 里没有 'P'"（最后那一遍里 enumMember 照常说话），
// 再是"枚举成员的值算不出来"。两句都对着这一处，不指别处。
enum A {
	X = B.P,
}

enum B {
	P = A.X,
}

int main() {
	printf("%d\n", A.X);
	return 0;
}
