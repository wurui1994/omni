// 给了 `-I` 还是找不着（第六十二刀）。与 bad/import-missing 是同一条边界的另一半：
// 那一条没给目录表，这一条给了一个（cases/incdirs/b，见 .args），spec 在旁边和那个目录里
// 都没有。
//
// 为什么还是"还不收"而不是硬错：jancy 那边这是 `import '%s' not found`
// （jnc_ct_ImportMgr.cpp:122）—— 但它在翻文件系统**之前**先翻扩展库里嵌着的那份源码表
// （findSourceFileContents，jnc_ct_ImportMgr.cpp:52-60），标准库那些名字它压根不去文件
// 系统。我们没有那张表，所以"给了 -I 还找不着"仍然分不出是"名字写错了"还是"这个名字本来
// 就该从扩展库里拿"。要分得出来，得先接 jancy 的标准库 —— 那是另一件事、另一刀。
//
// 拒的那句话里带**在哪儿找过**：这一格是第六十二刀加的，不然读的人分不出是名字错了还是
// 目录表少了一格。
import "no-such-lib-62.jnc"

int main() {
    return 0;
}
