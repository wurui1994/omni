// 第一百一十三刀的界（一）：装进 variant 的东西今天只收**整数 / 实数 / 布尔 / 枚举 / 字符串**。
// 指针那一格要 variant 里也装得下"三个字的 fat 指针"（第十六刀那一格），而载荷是一格 int；
// 结构体与函数值同理。语料里 variant 装的全是前五种（190 处逐处看过）。
int main() {
	int x = 5;
	int* p = &x;
	variant_t v = p;
	printf("%d\n", x);
	return 0;
}
