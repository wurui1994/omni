// 函数类型的 typedef（第三十八刀的边界）。`typedef int F(int);` 起的是个**函数类型**的名字
// —— 不是函数指针：那一格在 jancy 里写成 `typedef int function* F(int);`，第五十五刀已经
// 收了（见 cases/52-fnptr.jnc 的 BinOp）。剩下这一条是"函数类型自己"当一格名字，用处是
// 声明原型或者当属性的存取器签名，与函数指针不是同一格。
//
// 语法那一半早就能过：jancy 的 typedef 是存储类（DeclarationSpecifier.llk:83），后面跟的
// 声明符可以带函数后缀。所以拒在降级这一层，理由是"函数类型"，不是"语法不认"。
typedef int F(int);

int main() {
    printf("%d\n", 0);
    return 0;
}
