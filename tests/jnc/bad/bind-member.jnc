// 类的**成员**上的 bindable 属性（ADR-0016 第七十三刀的边界）。
// jancy 那边这一格是类里的一格字段（`Property::createOnChanged` 的头一支，
// jnc_ct_Property.cpp:131-134：`m_parentType` 非空就 `createField`），而这一层的事件是
// "元素是函数值的数组"，方言的结构体字段放不下函数值（与第五十七刀虚派发那条同一笔账）。
class C {
	int autoget bindable property m_p;
}

int main() {
	C* c = new C;
	c.m_p = 1;
	printf("%d\n", c.m_p);
	return 0;
}
