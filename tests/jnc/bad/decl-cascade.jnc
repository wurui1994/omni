// 那格声明**自己没成**，后面每一处用到它的地方（第二百三十五刀）。语料里的原样是
// `UsbDeviceParams const* params`（UsbLogRepresenter.jnc:19 —— 那个类型在没 import 的
// 另一个文件里，逐份编那笔口径账），一个形参就带出四五句"未声明的变量 'params'"。
//
// 那是认错人：名字明明写着，只是那一句先报了错、于是它一格都没登记上。而且把**一件事**
// 记成好几笔账（一个名字用了几处就几笔）。
//
// 记成**普通错**而不是"还不收"：根上那一句（`没有这个类型`）本来就是普通错，这儿只是它的回声。
int main() {
	MissingType params;
	params.m_a = 1;
	printf("%d\n", params.m_a);
	return 0;
}
