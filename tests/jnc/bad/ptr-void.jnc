// `void*` 上**读不了、下标不了、也不能做算术**（第二百六十一刀）—— jancy 自己就报错
// （"pointer to void"）：它没有元素类型，"一格跨多远"这句话在它身上没有意义。
//
// 那一族本身已经接上了（正面判据在 cases/203-voidptr.jnc）：接过来、传出去、`(T*)p`
// 换成一格有类型的指针，三样都行。这儿拦的是**第四样**。
int first(void const* p) {
	return *p;
}

int main() {
	int* q = new int;
	*q = 7;
	printf("%d\n", first(q));
	return 0;
}
