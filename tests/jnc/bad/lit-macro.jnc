// 预定义的字面量宏（第五十四刀的边界）。`__FILE__` / `__DIR__` / `__FUNC__` / `__LINE__` /
// `__DATE__` / `__TIME__` 在 jancy 的词法里**就是** LITERAL（jnc.grammar 的 keyword 那一行
// 记着这条），所以 `"file: " __FILE__ "\n"` 靠的正是相邻拼接（test116.jnc:35）。
//
// 拼接这一刀不带它们：那六个的值要靠**编译期环境**（源文件路径、当前函数名、行号），
// 而后两个（`__DATE__` / `__TIME__`）连"每次跑出来一样"都做不到 —— 期望输出没法写。
// 前四个真要接也得连"这一层怎么定义那个路径"一起定（绝对路径会让测试跟着机器走），
// 那是自己的一格决定，不是折字面量顺手能带的。
int main() {
	printf("file: " __FILE__ "\n");
	return 0;
}
