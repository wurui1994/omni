// ADR-0025 的 S2/S3 定的替换层次：替换只在 **type-spec 那一层**做，所以实参带 `*` 的
// 这一格（语料里是 `Array<Bucket*>`）明说不收 —— 混进来会成静默的错答案：`*` 落在
// 参数名那一格上，替换之后那颗 `(ptrs …)` 该挂在谁身上没定，是另一笔账。
struct Box<T> {
	T m_v;
}

struct Node {
	int m_n;
}

int main() {
	Box<Node*>* b = new Box<Node*>;
	return 0;
}
