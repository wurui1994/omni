// 两个 main（第六十刀）。import 进来的条目与写在这儿的条目是**平权的一堆** —— 所以"入口
// 只能有一个"这条规矩自然管到跨文件：那边一个、这边一个，就是定义了两次。
// 路径里的 `..` 也一并量到了：找法是"写这条 import 的文件自己的目录"加上这个相对路径。
import "../cases/imports/withmain60.jnc"

int main() {
    return 0;
}
