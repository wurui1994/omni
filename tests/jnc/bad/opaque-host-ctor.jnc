// `opaque class` 上声明的 construct，体也在宿主那边（ADR-0016 第六十六刀）。
// 这一条不能"当没有构造"悄悄放过去：那样交出来的对象是一格全零的内存，而 jancy 那边
// 宿主的构造是真跑了的（opaque.rst:69 那句 "only if their declaration includes
// **operator new**" 说的就是这一格由宿主提供）。少跑一段比报错难查得多。
//
// 体外真写了 `Lock.construct() { … }` 的时候这一条不响 —— 那就有实现了，见 cases/62-opaque.jnc。
opaque class Lock {
	int m_depth;

	construct();
}

int main() {
	Lock* p = new Lock();
	return 0;
}
