// `opaque class` 上声明的 construct **现在能降了**（第一百六十二刀）：`new Lock(…)` 是"造一格、
// 写 `$tag`、再 `(ccall Lock_construct self …)`"，与方法（ADR-0022 的 J4b）、属性的取/存
// （第一百六十刀）同一条约定。正面判据在 `tests/llvm/run.js` 第 9 节（那儿有宿主的体）。
//
// 这一条留下来钉的是**边界**：形参要落进 C_ABI 那几个词。**枚举现在能过了**（第一百六十三刀）——
// jancy 的枚举在代码生成这一层就是它的底整数（jnc_ct_EnumType.h:182-183）。落不进的是按值过去的
// **结构体**：那一格在 C 的调用约定里按大小拆寄存器/进栈（arm64 与 x86-64 还不一样），要先把
// 那套规则量出来才能发 —— 按一格指针糊过去是**猜**，而猜错的调用约定比报一句错难查得多。
struct Cfg {
	int m_a;
	int m_b;
}

opaque class Lock {
	int m_depth;

	construct(Cfg cfg);
}

int main() {
	Cfg c;
	Lock* p = new Lock(c);
	return 0;
}
