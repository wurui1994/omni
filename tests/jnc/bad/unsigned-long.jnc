// 无符号的 8 / 16 / 32 位已经接上了（第三十三刀）；64 位那一格没接。
//
// 划在这里的理由是**规范形**：u8 / u16 / u32 的值域都装得进方言那个有符号 64 位里，回卷之
// 后拿到的还是它本来的非负值，于是 `/` `%` `>>` `<` 用方言现成的有符号版就是对的。u64 不
// 是：4294967296 * 4 那种数在方言里只能以负数的样子存着，除、取模、右移、比较全都会给出
// 有符号的答案。要接它得先让方言长出无符号的那一半算子。
int main() {
    unsigned long x = 1;
    printf("%d\n", x > 0);
    return 0;
}
