// 第一百一十二刀的界（一）：位域**只在结构体的字段上**。局部量上写 `: 3` 在 jancy 那边
// 压根不是位域 —— 那条语法规则上挂着 `resolver({ return false; })`（Declarator.llk:494-499），
// 注释写得很清楚："宁可当条件表达式"（`p = new int : 3`）。这一层的语法也是这么排的
// （jnc.grammar:438 那句 `(prefer -1)`），所以这儿收下来的是**声明符后缀**，当场拒。
int main() {
	int x : 3;
	printf("%d\n", x);
	return 0;
}
