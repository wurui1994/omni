// 函数里的 `static`。这不是"模块级变量的另一种写法"—— decl_storage.rst 说 static 是
// "the memory for variable is allocated at the program start and it stays there until the
// program terminates"，而局部量的 local 存储是"every time intruction pointer goes through
// a variable declaration, this will be a **new** copy"。
//
// 接它要两样东西：一格程序启动时就分配好的存储（这个有，就是模块级变量那一格），加上
// **初值只跑一次**（jancy 那边是 `once` 的机制，cflow_once.rst："the compiler will
// generate a thread-safe wrapper … this code executes once per each program run"）。
// 后一样还没有，所以明着拒 —— 悄悄把它当普通局部量会给错答案（每次进来都重置成 0）。
int tick() {
    static int n = 0;
    n++;
    return n;
}

int main() {
    printf("%d %d %d\n", tick(), tick(), tick());
    return 0;
}
