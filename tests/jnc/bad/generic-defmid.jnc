// 第一百二十二刀的界：带默认类型的那几格必须**排在后面**。
// 不然"给了 2 个实参"落在哪几格上就没有唯一答案（C++ / jancy 那边同一条）。
// 这一条是**真错**，不是"还不收"。
struct Eq<T> {
	T m_t;
}

struct H<E = Eq<int>, K> {
	K m_k;
	E m_e;
}

int main() {
	H<int>* h = new H<int>;
	return 0;
}
