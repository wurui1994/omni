// 一个字段都没有的 `struct`（第五十二刀量出来的边界）。jancy 收它 —— `test/jnc/test97.jnc`
// 里的 `struct S1 { };` / `struct S2 { };` 就是空的，而那份不是 `fail` 用例。
//
// 卡点**不在这一层**：降级发出来的 `(struct S )` 是方言拒的（「结构体 'S' 至少要有一个字段」，
// sexpr/lower.js 里那一句）。要收它得让方言认"零个字段"的一格 —— 那牵着布局的尺寸
// （`sizeOf` 回 0）、`pnew` 一块零字节的内存、四个后端与两个解释器各自的结构体表示，
// 是方言那一侧的一刀。
//
// **类**那一边这一刀已经过去了：`class T {}` 发的是 `($hdr int)` 一格占位 —— 理由是
// jancy 的类**本来就有对象头**（01_Classes.jnc:12-14），一格占位比"零字节的对象"更贴它。
// struct 没有头，补一格就是在布局上说假话，所以这一边留着。
struct S {
}

int main() {
	S s;
	printf("ok\n");
	return 0;
}
