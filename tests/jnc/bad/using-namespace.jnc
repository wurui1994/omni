// `using namespace a;`（第五十一刀的边界）。命名空间这一刀做的是"名字有前缀、查名从里往外
// 退"，而 `using` 是**往当前这一格里塞一条别的查名路径**（jancy 的
// `Namespace::addUsingNamespace` / `NamespaceMgr` 里那张 using 表）—— 它把"从里往外退"
// 这条线性的路变成一张图，查名得连"哪几个 using 进来的命名空间里有同名的"一起判，
// 撞名了还要报歧义。那是另一件事，单独一刀。
//
// 同一档还有 `using extension` 与 `friend`：前者要先有 extension，后者要先有访问控制 ——
// 这一层两样都没有（`public:` / `protected:` 也还不收）。
namespace a {

int f() {
	return 1;
}

}

using namespace a;

int main() {
	printf("%d\n", f());
	return 0;
}
