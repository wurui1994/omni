// switch 摊出来的那圈循环是**合成的**（第三十六刀），所以里面的 `continue` 会跳到它头上
// 而不是外层那个真循环 —— 那是个死循环。与其给个错答案，在这儿停下。
//
// 要接它得给方言加**带层号**的 `cont`（jancy 自己有 `continue2`，cflow_switch.rst:37 那段
// 演示的就是 `break2` 从 switch 里穿到外层循环），那是另一刀。
int main() {
    int i = 0;
    while (i < 3) {
        i++;
        switch (i) {
        case 1:
            continue;
        default:
            printf("%d\n", i);
        }
    }
    return 0;
}
