// 属性上的复合赋值（ADR-0016 第六十八刀的边界）。
// jancy 那边 `p += 1` 是"调一次取值器、加一、再调一次存值器"，而这一层的赋值降成方言的
// **一句** —— 两次调用摆不进去。要接它得先能把一条赋值摊成几句（与第六十四刀 EC_HOIST
// 那格是同一类事）。
int property g_p;

int g_p.get() {
	return 1;
}

g_p.set(int x) {
}

int main() {
	g_p += 1;
	return 0;
}
