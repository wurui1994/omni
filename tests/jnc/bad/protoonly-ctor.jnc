// 第一百四十八刀的界：类体里 `construct` **只有原型**，造一格它的对象。
//
// 与第一百四十七刀是同一笔账的另一半（那边是普通方法）。先前这一层报的是
// "'Widget' 的 construct 要 0 个实参，这里给了 1 个"——那个 0 是**这一层自己合出来的**那格
// 无参构造，也就是拿自己合的东西去顶用户声明的那一个，于是话说成了"你多给了实参"。
// 真拦路的是：construct 在，体不在这儿。
//
// 判据两问（少一问就会把 cases/50-construct、62-opaque、63-dualmod 三份拒掉）：
//   1. 那个类的 construct 只写了原型；
//   2. **而且**体外也没有定义（`C.construct(…) { … }` 是第五十三刀那条正当的放法）。
// 这一层自己合出来的那格带 `synth` 记号，不算"有定义"。
//
// 语料里的原样：`class GroupProperty: Property { construct(string_t name); }`
// （ui_PropertyGrid.jnc:37-39）—— ui 那一族的属性类几乎每一个都是这个形状。

class Widget {
	int m_n;

	construct(int n);
}

int main() {
	Widget* w = new Widget(3);
	printf("%d\n", w.m_n);
	return 0;
}
