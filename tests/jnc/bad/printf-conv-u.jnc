// printf 的**转换字符**那一族里最后两条（第三十一刀把 `%g` / `%G` 收了）：`%u`（无符号
// 十进制）与 `%p`（印地址）。
//
// `%u` 量下来**不要方言长任何东西**：C 把实参当无符号读，位数是**默认实参提升之后**那一格 ——
// 与 `%x` 一字不差（`char c = -56; printf("%u", c)` 是 `4294967240`，量过）。方言的
// `(sbase E 进制)` 本来就把实参当无符号 64 位读、进制那一格也不限于 8 / 16，所以 `%u` 就是
// `%x` 那条路子换成 `(sbase … (int 10))`。精度照 `%d` 那一套（`%.3u` 印 7 是 `007`），
// `+` / 空格 / `#` 在它上面都是 C 的**未定义行为**（clang: "flag '#' results in undefined
// behavior with 'u' conversion specifier"）。这一条便宜，但它是**下一刀**的事 ——
// 一刀一件事，这一份先把它记在这儿。
//
// `%p` 是另一回事：它要"印地址"这件事本身，而这一层**刻意不许观测裸地址**（ADR-0016 决策一
// —— 五条腿里三条是 arena 模拟、两条是真指针，裸地址在两边根本不是同一个数）。所以它不是
// "还没做"，是**这一层不做**。
//
// 拒在这儿而不是猜一个：`%u` 印成 `%d` 的样子会给一个静默错的答案（负数上差 2^32）。
int main() {
    printf("%u\n", 7);
    return 0;
}
