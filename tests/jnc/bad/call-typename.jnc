// 拿一格**类型**当函数调（第二百三十三刀）。jancy 里 `T(实参…)` 是一格**构造式转换**
// —— 与 `(T)值` 那种写法是同一件事，只是它按目标类型去挑一条转换（可以不止一个实参）。
//
// 先前这一层报的是"没有这个函数：'S'"，那是**认错人**：那个名字明明在，只是它不是函数。
// 这一层只收 `(T)值` 那一种写法，所以这一格明说是哪一件事没收。
//
// G-001 那一刀之后话又准了一步：`T v = T(实参…)` 这一种形状现在走"就地构造"那条路
// （与 `T v(实参…)` 同一件事），所以这一份量到的是**这个结构体没有 construct** ——
// 比"构造式转换整件事没收"更准（那一句留给别的形状：不同类型之间的 `T(x)`）。
struct S {
	int m_a;
}

int main() {
	S s = S(1);
	printf("%d\n", s.m_a);
	return 0;
}
