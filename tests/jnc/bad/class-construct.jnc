// 类的**构造与析构**（第五十二刀的边界）。jancy 的写法与 C++/Java 都不一样：关键字就叫
// `construct` / `destruct` / `static construct`，而且可以重载（01_Classes.jnc:32-49）；
// 变量声明上还能带实参（`C1 c(100, 200);`，同一份 94 行）。
//
// 这一刀不收它，理由是它牵着的不止一个动作：
//   - `C c;` 要在那一格造出来之后**接着调**构造，而带实参那一种的语法是"声明符上的构造子"
//     （这一层的 declarator 现在把它整个当"C++ 式的构造声明符"拒掉）；
//   - 析构要有"作用域退出时按逆序调"，那是方言里没有的一层；
//   - `static construct` 是"整个类一次"，要一格模块级的闸门（第二十六刀那个 once 的形状）。
// 三样各自都是一刀的量，所以这里一起留着 —— 半条构造比没有构造更糟。
class C {
	int m_x;

	construct() {
		m_x = 7;
	}
}

int main() {
	C c;
	printf("%d\n", c.m_x);
	return 0;
}
