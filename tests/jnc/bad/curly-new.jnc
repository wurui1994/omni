// `new T { … }`（decl_curly.rst 最后那一格：`Point* point2 = new Point { m_y = 2000 }`）。
//
// 花括号初值是**几条语句**：先把那一格内存开出来，再逐格写。声明与赋值那两处是语句位置，
// 所以那两处收（第十四刀）；`new T { … }` 是**表达式**，而这一层的表达式降级只交出一段
// 文字（`{code, type}`），没有"顺带发几条语句"的通道 —— `while (new T { … })` 那种位置
// 连"提到前面去"都不成立（条件每一圈都要重算）。
//
// 要它得先在表达式降级里开一条语句通道。写成 `T v = { … }` 再取 `&v` 是同一个东西。
struct Point {
    int m_x;
    int m_y;
}

int main() {
    Point* p = new Point { m_y = 2000 };
    printf("%d\n", p.m_y);
    return 0;
}
