// 第一百三十四刀的界：`operator *` 回的不是指针。
// 这一层收下它之后接着走的是"指针取字段"那条原有的路（`*it` 之后可能跟 `.m_x`），所以要它
// 回一格指针。jancy 那边回什么都行 —— 接下来那一步按回的类型再解析一遍，那要一整套
// "重载之后再解析"的路。明说不收，不猜。
struct It {
	int m_n;

	int operator * () {
		return m_n;
	}
}

int main() {
	It it;
	printf("%d\n", *it);
	return 0;
}
