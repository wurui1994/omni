// 结构体字段的默认值 —— 这一层不收（第七十八刀划的那条界）。
//
// jancy 那边它是成立的：初值同样由 `MemberBlock::initializeFields` 重放，结构体没有构造
// 就合成一格（jnc_ct_DerivableType.cpp:465-472、909-927），嵌套的结构体递归调。
//
// 而这一层的结构体是**纯值**：`S s;` 只降出一格内存，没有"造出来的时候"这个时刻可以插代码 ——
// 按值传、按值回、花括号初值、数组里的元素、结构体里的结构体，每一处都是一格新的 S。
// 要收下来就得先给结构体长出构造那条路（那是自己一刀）。收下来却不发那几句赋值是个静默的
// 错答案，所以明说。类的那一格已经落了，见 cases/75-fielddefault.jnc。
struct S {
	int m_x = 5;
}

int main() {
	S s;
	printf("%d\n", s.m_x);
	return 0;
}
