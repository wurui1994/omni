// 第一百〇六刀留下的边界：**结构体**里的 typedef 不提到顶层（与第一百〇一刀同一个决定 ——
// typeDecl 就地报的话更准，提上来同一件事会报两遍）。所以它落在"结构体里除字段以外的成员"
// 那一句上。类里的那一格收了，见 `cases/97-classtypedef.jnc`。
struct Box {
	typedef int Num;

	int m_n;
}

int main() {
	Box b;
	b.m_n = 5;
	printf("%d\n", b.m_n);
	return 0;
}
