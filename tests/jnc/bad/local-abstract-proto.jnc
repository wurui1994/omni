// 函数体里的 `abstract` 原型（矩阵 `fn-body × method-abstract` 那一格挑出来的一刀）。
//
// 先前这一句报的是 "'abstract' 只能写在类的方法上（type_class.rst:178）" —— **认错人**：
// 那句话说的是"这个词写错地方了"，可真身与 `bool errorcode f(int);`（第二百六十二刀）一样，
// 是"函数体里写了个原型"。jancy 的函数体里写不了原型（那要一层"块作用域也是命名空间"），
// 所以这一格照旧不收，只是话要说准 —— 同一格语法在函数体里有两种读法，一起说。
//
// 反过来那一格没动：`virtual int v = 1;`（**没有形参表**的一格局部量）照旧报
// "只能写在类的方法上" —— 那时那句话是对的（与模块级那一处同一条口径）。
int main() {
	abstract int probeA();
	return 0;
}
