// 编译期求值里移位量越界（第四十八刀的边界）。C 里移位量 >= 左操作数的位宽是**未定义行为**
// （C99 6.5.7p3），所以没有可对的答案 —— 与 bad/printf-plus-hex 的 `%+x`、bad/bitflag-neg
// 的负 bitflag 同一类。这一处的判据是 0..63（方言那一格整数最宽 64 位）。
//
// 折得动的那些在 cases/45-constfold.jnc 里：`1 << 4`、`(1 << 4) - 1`、`100 / 7` 等等。
// 除以 0 也落在这条边界上（同一句诊断）。
enum Bad {
	A = 1 << 64,
}

int main() {
	printf("%d\n", Bad.A);
	return 0;
}
