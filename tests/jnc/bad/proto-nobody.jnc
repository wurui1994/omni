// 顶层的原型**没有**带体的定义（ADR-0016 第九十二刀那条边界）。
// jancy 那边这一句是合法的声明，实现可以在别的编译单元 / 宿主那边；这一层的一趟降级只有
// 这一份源码（加 import 摊进来的那些），所以那一格函数没处来。
// 实现在宿主那边的走 opaque class 那条路（第六十六刀 + ADR-0022 的 J4b）。
//
// 第一百五十一刀之后**两处**都说这一句：声明那儿一条，`missing(1)` 那个调用点也一条。
// 先前调用点报的是"没有这个函数：'missing'"—— 那是认错人（那个名字明明声明过），
// 与第一百四十七刀给类里那种原型定的说法是同一件事。
int missing(int x);

int main() {
	printf("%d\n", missing(1));
	return 0;
}
