// 结构体按值传形参。抄一份的机制这一层有（copyAgg，第十二刀就是靠它做出值语义的），
// 缺的是**调用约定**那一半：谁来开被调那一格内存、什么时候抄。
//
// 方言的形参是一个值，而这一层的结构体"值"是它那段内存的地址 —— 直接把地址传过去就是
// **按引用**传，改形参会改到调用方那一份，与 C/jancy 的语义正好相反（会给错答案，不是
// 拒不了）。所以这一格明着拒：要传就写 `Inner*`（那才是"按引用"，写出来就看得见）。
struct Inner {
    int y;
}

void bump(Inner s) {
    s.y = 99;
}

int main() {
    Inner a;
    a.y = 1;
    bump(a);
    printf("%d\n", a.y);
    return 0;
}
