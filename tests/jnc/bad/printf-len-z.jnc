// `%zd`（第四十四刀的边界）。`z` 的宽度是平台的 `size_t` 定的 —— 这一层没有"跟着平台走"的
// 那一格（方言里整数只有固定的四档），所以收它就得先决定 `size_t` 是什么，那是另一件事。
//
// 同一格里的还有 `%jd` / `%td`（`intmax_t` / `ptrdiff_t`）、`%Lf`（long double）、`%llf`
// （C 里本身就没定义）、`%lc` / `%ls`（宽字符那一族）。
//
// 语料里这几个一处都没有（量过：`%zd` / `%zu` / `%jd` / `%Lf` 各 0 处），所以这条边界
// 不欠 jancy 什么。
int main() {
    int n = 42;
    printf("%zd\n", n);
    return 0;
}
