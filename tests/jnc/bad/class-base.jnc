// 类的**基类**（第五十二刀的边界）。jancy 的继承是多继承 + 虚表那一整套：
// `basetype1..9` 拿基类那一格、`virtual` / `abstract` / `override` 管虚函数、
// 类的头部里有一格 vtable 指针（01_Classes.jnc:12-14 那段注释就是它）。
//
// 这一刀的类是"一格 `(struct …)` + 变量里放地址"：没有对象头、没有虚表、方法就是名字带
// 前缀的自由函数。接基类要先有那两样（字段布局得把基类那一段摆在前面、方法得能按对象里
// 那一格 vtable 分派），所以这里是"还不收"，不是"你写错了"。
//
// 同一档还有 `opaque class`（它连字段都不给看，要一层"只有这个模块知道布局"的间接）。
class Base {
	int m_x;
}

class Derived: Base {
	int m_y;
}

int main() {
	Derived d;
	d.m_x = 1;
	printf("%d\n", d.m_x);
	return 0;
}
