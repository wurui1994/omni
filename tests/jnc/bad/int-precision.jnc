// C 里精度对**整数**不是"小数位数"，是"至少几位数字"（`%.3d` 印 `007`）；对 `%s` 是
// "最多取几个字符"。两件与小数无关的事，各要自己的一段 —— 第八刀只做了 `%f` 那一格。
//
// 拒在这儿而不是当成 `%d` 忽略掉：忽略的话 `%.3d` 会印 `7`，那又是一处静默的差别。
int main() {
    printf("%.3d\n", 7);
    return 0;
}
