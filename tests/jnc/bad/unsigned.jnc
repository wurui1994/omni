// `unsigned` 以前是**静默忽略**的 —— 在没有位宽的时候那还看不出来，有了位宽之后它会
// 直接给错答案：下面这一行该印 200，忽略修饰符的话按有符号 8 位算，印 -56。
//
// 所以这一条从"静默忽略"改成"当场拒"。要接它得先有无符号那一半的位宽规则：回卷是
// `x & M`（不摊符号位）、`/` `%` `>>` `<` 都换成无符号那一版、`%d` 也要跟着改。
int main() {
    unsigned char c = 200;
    printf("%d\n", c);
    return 0;
}
