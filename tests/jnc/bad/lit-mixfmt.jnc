// 普通字面量与 `$"…"` **混着**拼（第二百四十九刀的边界）。
//
// 几段都是 `$"…"` 的那一种收了（见 cases/187-fmtconcat）：文本首尾相接、序号共用一格。
// 混着的这一种还不收 —— jancy 那边普通那一段进的是同一个 Literal 缓冲，可它里头的 `%`
// 该不该当转换看的是"整串有没有格式化的成分"，而这一层的 `%` 是逐段扫的（普通字面量那一段
// 里的 `%` 已经被当成文本处理过一遍了）。要接得把两种段的转义与 `%` 口径先并成一张表。
int main() {
	// 走的是"拼接"那条路（不经 printf —— 那儿另有一句自己的话）
	string_t s = "a" $"b%1\n"(42);
	printf("%s", s);
	return 0;
}
