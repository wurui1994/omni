// `threadlocal`（decl_storage.rst 的第三种存储类）。第二十六刀把 `static` 的局部量收了，
// 那一条边界就落到这儿：threadlocal 是"a separate copy of this variable for every thread"，
// 要线程本地存储 —— 这一层从下到上一个线程都没有（四条腿里没有一条能开线程），
// 所以它不是"少写几行"的事。
//
// 文档自己还给它记了两条限制（"threadlocal variables can not have initializers"、
// "can not be of aggregate type"），接它得连那两条一起接 —— 而这两条正说明它跟 static
// 不是同一格东西：static 那一格是唯一的、初值包在 once 里（第二十六刀落的就是这个）。
int next() {
    threadlocal int n;
    n++;
    return n;
}

int main() {
    printf("%d\n", next());
    return 0;
}
