// printf 的**转换字符**那一族里剩下的三条（第三十刀把 `%e` / `%E` 收了，边界就落到这儿）：
// `%g` / `%G`（`%e` 与 `%f` 里短的那个）、`%u`（无符号十进制 —— 与 `unsigned` 那条边界
// 同一格）、`%p`（印地址 —— 而这一层刻意不印裸地址，见 ADR-0016 决策一）。
//
// `%g` 看着最近：方言的 `(tostr E N)` 就是 `%.Ng`，平常那些值上量下来**逐字节相同**
// （`1.5` / `0.0001` / `1e-05` / `123456` / `1.23457e+06` / `100`，`%.3g` 的 1234 是
// `1.23e+03`，都对得上）。可它不是同一个算子，有三处量出来的差别：
//   - `%#g` **不去尾随零**（C 那边 `%#g` 印 1.5 是 `1.50000`，`(tostr 1.5 6)` 是 `1.5`）；
//   - C 里精度 0 **等于 1**（`%.0g` 印 1.5 是 `2`），而 `(tostr E 0)` 不是那个意思
//     —— 现在的实现在这一格上直接抛（native.js 的 `toExponential(-1)`）；
//   - `(tostr E N)` 的 N 只收**字面量**（见 tests/sexpr/bad/tostr-digits），所以 `%.*g`
//     这一行落不下来 —— `sfix` / `ssci` 是第二十八、三十刀才放开那一格的。
//
// 所以 `%g` 要方言再长一格（`%.Ng` 的"带 `#` 那一支"加上运行期的 N），不是把 `tostr` 直接
// 拿来用。拒在这儿而不是猜一个：印成 `%e` 或去错了零都会给一个静默错的答案。
int main() {
    printf("%g\n", 1.5);
    return 0;
}
