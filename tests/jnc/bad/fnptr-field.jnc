// 函数指针的**字段**（第五十五刀的边界）。jancy 的类里放一格 `function*` 是常见写法 ——
// 事件回调、`property` 的存取器、以及虚方法那张表，落下来都是"字段里躺着一个函数值"。
//
// 卡的是方言：量过一遍，`(struct Box (f (fnty (int) int)))` 在五条腿上都被拒
//（「结构体 'Box' 里有落不进内存的字段」，hir/types.js 的 structLayout），而方言的
// **class** 字段收（tests/sexpr/cases/15-fnvalues.sx 的 Holder 就是它）。这一层的类与
// 结构体都落成 `(struct …)` + `pfield`/`pload`/`pstore`（第五十二刀是这么选的：方言的
// `(class …)` 字段放不下 `(blk T N)`），所以两边各缺一半。
//
// 补法有两条，选哪条要量：让 structLayout 认函数值这一格，或者让方言的 class 字段也收
// `(blk T N)` 再把这一层的类搬过去。虚派发那一刀撞的就是这一格。
class Box {
	int function* m_op(int, int);
}

int main() {
	printf("%d\n", 0);
	return 0;
}
