// printf 的**转换字符**那一族里剩下的（第二十七、二十八刀把标志、宽度、精度收齐了，
// 边界就落到这儿）：`%e` / `%E`（科学记数）、`%g` / `%G`（`%e` 与 `%f` 里短的那个）、
// `%u`（无符号十进制 —— 与 `unsigned` 那条边界同一格）、`%p`（印地址 —— 而这一层刻意
// 不印裸地址，见 ADR-0016 决策一）。
//
// 每一条都记着该怎么长：`%e` / `%g` 要方言里一条"按 C 的 %e / %g 排版"的算子
// （`(tostr E N)` 是 `%.Ng` —— 形状很近，可它去尾随零、而 `%g` 的 `#` 标志才不去，
// 所以不是同一个东西）；`%u` 要无符号那一半的位宽规则；`%p` 要"印地址"这件事本身。
//
// 拒在这儿而不是猜一个：`%e` 印成 `%f` 的样子会给一个静默错的答案。
int main() {
    printf("%e\n", 1.5);
    return 0;
}
