// `bitflag enum` 里"再往上一位就没了"（第四十七刀划的界，第一百四十二刀**把它挪到了对的那一格
// 上**）。`A = -1` 在 32 位那一格里是全一 —— 它自己是个**好值**，收；拦的是 `B`：它要自动取值，
// 而"最高位再往上一位"出了这一格的宽度。
//
// jancy 算下一个成员的那一句是 `value = value ? 2 << sl::getHiBitIdx64(value) : 1`
// （jnc_ct_EnumType.cpp:305）。`getHiBitIdx64` 收的是 uint64_t，全一那一格回 63，于是
// `2 << 63` 移出去之后是 0 —— 那会把 `B` 与别的成员悄悄取成同一个值。宁可明说。
//
// 先前这一句拦在 `A` 上、话是"里的负值 —— 没有可对的答案"：那是把"下一个算不出来"记到了
// "这一个"头上，而且按 signed 判负这一步本身也不对（jancy 那一句从来没见过负数）。
// 见 ADR-0016 第一百四十二刀。正数那一格全收（见 cases/44-bitflag.jnc）。
bitflag enum Neg {
	A = -1,
	B,
}

int main() {
	printf("%d\n", Neg.A);
	return 0;
}
