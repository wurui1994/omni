// 第一百一十九刀的界：递归的闸门。这一格的实参每实例化一层就长一圈
// （`L<int>` -> 体里 `L<L<int> >` -> 体里 `L<L<L<int> > >` …），名字每次都是新的，所以
// "同一份实参不重复造"那条 memoise 停不下来 —— 非得有一道深度闸门（`TMPL_DEPTH = 8`）。
//
// 停在**一句说得清的话**上，而不是把内存撑爆或者转半天：这一条与别的界同一条纪律。
// 语料里量到的最深是 3 层（`Iterator<RbTreeNode<K,V> >` 落在 `MapImpl<…>` 的基类表里），
// 闸门放在 8 是它的两倍多 —— 挡的是发散，不是挡人写深。
struct L<T> {
	L<L<T> >* m_next;
	T m_v;
}

int main() {
	L<int>* l = new L<int>;
	return 0;
}
