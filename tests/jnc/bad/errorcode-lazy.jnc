// errorcode 的传播插不进去的位置之一（第五十八刀）：`&&` / `||` 的**右边**。
//
// 传播那两句（"抬一格临时、比一下、等于出错值就 return"）只插得到**这条语句之前**。而
// `&&` 的右边求不求值要看左边 —— 抬到语句之前就成了"无条件先调一遍"，短路语义与求值顺序
// 一起没了。jancy 那边不受这一格限制：`checkErrorCode` 是把当前基本块**当场切开**的
// （jnc_ct_OperatorMgr_Call.cpp:591-592），惰性那一支自己就在一个块里。
//
// 所以这一条要的是"给表达式做一次真正的三地址化"（把惰性那几支各自摊成块），不是这一刀。
// 表达式位置上的 `? :` 两支同一格；循环的条件也同一格（那儿是"每圈重求一次"）。
int errorcode fetch(int i) {
	if (i < 0)
		return -1;
	return i * 10;
}

int errorcode lazy(int i) {
	if (i > 0 && fetch(i) > 5)
		return 1;
	return 0;
}

int main() {
	printf("%d\n", try lazy(2));
	return 0;
}
