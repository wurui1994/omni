// 找不着的 import（第六十刀）。jancy 那边这是一条硬错（"import '%s' not found"，
// jnc_ct_ImportMgr.cpp:122）；这一层记成"还不收"。
//
// 第六十二刀之后这一条的理由窄了一格：`-I` 那个开关有了（见 cases/59-incdir.jnc），
// 所以它量的是**一个目录都没给**时的那句话 —— 得说清"只在旁边找过"。给了目录还找不着是
// 另一条（bad/import-missing-inc.jnc），那一条的理由是"我们没有扩展库里嵌着的源码表"。
import "no-such-lib-60.jnc"

int main() {
    return 0;
}
