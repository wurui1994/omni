// `bitflag enum`（第三十九刀的边界）。它是枚举的另一格：取值自动是 1、2、4、8……而不是
// 0、1、2，而且 `|` 与 `&` 的结果仍然是那个枚举类型本身，还允许与 0 隐式互转
// （type_enum.rst:66-73）。这几条规矩得连着 bitflag 一起接，所以这一刀先只收普通枚举。
//
// 语法那一半能过 —— `bitflag` 是修饰符（DeclarationSpecifier.llk 里的 TypeModifier），
// 所以拒在降级这一层。
bitflag enum Flags { A, B, C }

int main() {
    printf("%d\n", 0);
    return 0;
}
