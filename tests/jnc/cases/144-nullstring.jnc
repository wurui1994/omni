// `null` 当一格 `string_t`（第一百六十五刀）。语料里的原样：
//   `storage.writeString($"%1-key-%2"(name, i), null);`（test/ioninja/api/ui_Dictionary.jnc:64
//   —— 那一格形参声明的就是 `string_t`），逐份榜上 `null 得从左边知道自己是哪种指针` 那 104 处
//   里最常见的一种。
//
// jancy 的 `string_t` 是一格带指针的结构（`jnc_String`：`m_p` / `m_length` …），`null` 进去就是
// "指针那格是空的那一种字符串"。这一层**不用另造一格**：字符串槽的零值本来就是 `(str "")`
// （`string_t s;` 那一格出来的就是它），而这一层可观测的三件事在"空的"与"零长"上一模一样 ——
// 长度是 0、当条件用是假（第一百四十六刀那条 `slen != 0`）、印出来什么都没有。
//
// 期望输出的出处：/tmp/c150.c（`cc -O0 -std=c99 -Wall`）—— 独立的一份：C 里 `NULL` 与 `""` 印
// 出来当然不一样，所以那一份按 jancy 的语义**手写成零长的那一格**，再把三件事逐个印出来。

void take(string_t s) {
	printf("[%s] empty=%d\n", s, s ? 0 : 1);
}

int main() {
	take(null);

	string_t s = null;
	take(s);

	// 赋回一个真串，再赋回 null：两边都走得通
	s = "abc";
	take(s);
	s = null;
	take(s);
	return 0;
}
