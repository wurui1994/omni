// 取值器返回类型上那个 `const` **不是**属性的 `const`（第二百二十七刀）。语料里的原样：
//
//   property g_prop {
//       char const* get();
//       void set(int);
//   }
//
// （jnc_sample_01_export_c/script.jnc:29-33，`02_export_cpp` 同处）。那个 `const` 说的是
// "指到的字节不许改"，而属性能不能写按 jancy 的规矩只看一件事：**有没有存值器**
// （prop.rst:17 那句 "If a property has no setters then it is a const property"）。
//
// 先前这一层把属性的类型从取值器那份说明符抄过来时，`const` 也跟着抄进了改写出来的简单声明式，
// 于是 `pi.cst` 为真、写它当场被拒 —— 那是**拒错了**（jancy 收这两句）。改法：体里有存值器
// （自己写的或者只有原型的那种）时，把抄过来那份说明符上的 `const` 摘掉。
//
// `string_t const property m_string { … }` 那一种（体里直接就是取值器的体）**不摘** ——
// 那儿的 `const` 是属性自己那份说明符上的，正是"没有存值器"的意思（`bad/prop-noset.jnc`
// 那道墙钉的就是它）。
//
// 判据是手写的 C 双胞胎 /tmp/c183.c：把属性摊成"两个函数 + 一格存储"（这一层的落法就是这样），
// 取值器回 const 的值、存值器照常写 —— 两边都印 42。
int g_store;

property g_prop {
	int const get() {
		return g_store;
	}

	void set(int x) {
		g_store = x * 2;
	}
}

int main() {
	g_prop = 21;
	printf("%d\n", g_prop);
	return 0;
}
