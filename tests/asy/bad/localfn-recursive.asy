// 函数体里的函数抓外层的量、而它**真的**是递归的：这一层的名字是体降完才绑上的，
// 递归得先有那一格。asy 收（那边的名字在自己的体里就已经在了），我们还不收。
// 与 cases/128 那一档分得开：那边体里那个同名的名字是**另一份签名**，降得通。
void g(int k) {
  int down(int n) { return n <= 0 ? k : down(n-1); }
  write(down(3));
}
g(9);
