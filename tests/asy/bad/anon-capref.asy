// 捕获一个**会被改的**外层变量：asy 收，我们不收。
// 量过 `asy -noV`：这个文件原样跑下来印 100 退 0 —— asy 的捕获是**按引用**的，
// 闭包看到的是 m 后来那个值（99+1）。我们的 `(mkclo …)` 是按值抓一次（ADR-0010：
// 捕获是记录的字段，造闭包那一刻求一次），所以收下来会印 6，那是**错答案**。
// 所以这一条明着拒。外层名字没被赋值过的情况是通的，见 cases/47-anonfn.asy；
// 文件级的名字也通（那是 `(var 符号)`，本来就活读）。
int mut(int n) {
  int m = n;
  int h(int) = new int(int x) {return x + m;};
  m = 99;
  return h(1);
}
write(mut(5));
