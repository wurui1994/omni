// 同一层里用**另一个类型**重新声明同名的变量：asy 收（新开一格，把旧的那格遮住，
// 量过 `int x=1; string x="a"; write(x);` 印 a），我们还不收。
// 差别在表示上：这一层的 `(var 名字)` 就是那个名字，没有"同名两格"这一说 ——
// 类型相同时复用同一格就够了（旧那格再也取不到名字），类型不同时得**真的改名**，
// 而名字的用处遍布 nameOf / dotQual / 赋值 / 捕获，那是另一刀。
void f() {
  int x = 1;
  string x = "a";
  write(x);
}
f();
