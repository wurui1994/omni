// 函数里用文件级的 **pair**（同理还有 struct 与数组）：第二十四刀的模块级变量只收
// int/real/bool/string。pair 是 `(vec real 2)`、记录是类、数组是 `(arr T)`，它们的
// **身份**不在 MIR 的 8 位类型码里（身份在类型池的 aux 上），而全局池现在只有
// 「名字 + 类型码」这一列 —— 带身份的全局要先给那张池子加一列，那是另一刀。
// 文件级**照旧能用**（它还是 `(main …)` 的局部量），只是函数里看不见。
pair z = (1, 2);
real len() { return abs(z); }
write(len());
