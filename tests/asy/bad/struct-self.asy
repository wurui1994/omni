// 自引用字段：asy 收，我们不收。
// 量过 `asy -noV`：这个文件原样跑下来印 `0` 退 0 —— asy 的字段是懒的，next 搁着不造。
// 我们的隐式 operator init 要把内嵌的记录**造出来**（第十七刀加的），自引用就是无限递归，
// 所以拦在字段类型那一关。等哪一刀把"空引用 + 懒构造"做进来，这条边界才该挪。
struct A { A next; int x; }
A a;
write(a.x);
