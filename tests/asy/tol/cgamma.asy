// 复数 gamma（runpair.in:28 的 Lanczos g=7，九个系数照抄），连同它要的复数 exp / log
// 与复数幂（`std::pow(complex,complex)` 就是 `exp(w*log(t))`）。
// examples/gamma3.asy:12 的 `gamma(z)` 点名要它。
//
// **量出来两格超出这一节的容差**，所以不摆在下面：
//   gamma((1.5,0.5)) 的虚部：asy 0.0274250854138825、这一层 0.0274250854138827
//   gamma((5,2))   的虚部：asy 1.0575920372152  、这一层 1.05759203721522（差 2e-14）
// 这一节的容差是印出来的最后一位差 1.5 个步长，上面第二格是 2 个。理由不在这份转写上：
// 这一族是**超越函数的复合**（exp / log / atan2 各转手一次宿主的数学库，libm 与 V8
// 两边都不是正确舍入的），误差会攒。所以那两个点记在这儿，用例只钉容差之内的那几格。
write(gamma((4.5,0)));
write(gamma((0.25,0.75)));
write(gamma((-1.5,0.5)));
write(abs(gamma((4.5,0))));
write(exp((1.0,2.0)));
write(log((3.0,4.0)));
write(gamma((2,0)));
write(gamma((3.5,0)));
