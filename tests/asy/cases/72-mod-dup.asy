// 两个模块里同名的 struct（原来在 `bad/mod-dup` 里 —— 第三十八刀之后不再是"还没做"）。
//
// 核心方言的 class 名、方法名、构造函数名都按记录名拼，所以**真名**必须全局唯一 ——
// 以前撞上就拒（"两个模块里都有 struct 'Dup'"）。现在撞上就打散：源码里那个名字只当
// 「在这个单元里叫什么」的键，真名按单元前缀走。
//
// 后半段钉的是**后来的盖住先来的**：两条 import 都有 `Dup`，这个名字指的是**后**一条
// 那份（量过 asy 印 5；以前 modMerge 是"有了就不覆盖"，指的是前一条那份）。
import mod_dupa;
import mod_dupb;
write(1);

Dup d = new Dup;
d.y = 5;
write(d.y);
