// 折不动的那一份（与 y/ybad.y 同一个位置）：正则的字符类。
//
// 原型是 rose-exper-c++-compiler.ebnf:891 —— 那一份自己加的扩展：
//
//   Pragma-line ::= "#pragma" [^\n]+ //~[\n]* [\n]
//
// 判据是**当场报不支持，且指到那个字符**，不许折成一个近似的模式：字符类是词法层的东西，
// 而 .ebnf 这一格不带词法（ebnf.js 文件头第二处定义）。猜一个近似的会把语法静悄悄读歪。

pragma-line ::= "#pragma" [^\n]+

unit ::= pragma-line
