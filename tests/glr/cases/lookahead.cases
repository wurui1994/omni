;; `a c d` / `a c e` / `b c d` / `b c e` 走的是同一个状态里那对**归约/归约**冲突的两支：
;; 读到 `c` 时既能归约成 A 也能归约成 B（LALR 把从 `a` 来与从 `b` 来两条路的前看集
;; 合并了，见语法那份注释）。四条都必须过 —— 少过一条说明驱动在冲突处只挑了一支，
;; 那就是退化成 LR 了。
;;
;; 树里那个 `(A)` / `(B)` 正是"哪一支活下来了"：`a c d` 只有 A 那支能收尾，
;; `a c e` 只有 B 那支能收尾。所以这四行同时钉住了"死掉的那支真的死了"。
a c d            ==> (ad (A))
a c e            ==> (ae (B))
b c d            ==> (bd (B))
b c e            ==> (be (A))

;; 两支都活不到最后的那些
a c              !!> unexpected end of input
a d              !!> unexpected
