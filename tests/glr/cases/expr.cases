;; 每行一条：`输入 ==> 期望的 s-expr`，或 `输入 !!> 期望的诊断片段`。
;; 期望值是**手算**的 —— 这条轴没有外部参照，参照就是"优先级该这么算"。

1+2*3            ==> (add 1 (mul 2 3))
1*2+3            ==> (add (mul 1 2) 3)
1-2-3            ==> (sub (sub 1 2) 3)
2^3^2            ==> (pow 2 (pow 3 2))
(1+2)*3          ==> (mul (add 1 2) 3)
1.5+2            ==> (add 1.5 2)
1 ;; 行注释吃到行尾 ==> 1

;; 一元负号的优先级（UMINUS）比 `^` 还高，跟 bison 里那条 `%prec UMINUS` 写在最后一行
;; 的效果一样：所以是 `(-2)^2` 而不是 `-(2^2)`。这不是笔误，是这份语法的定义。
-2^2             ==> (pow (neg 2) 2)
-1*2             ==> (mul (neg 1) 2)
1--2             ==> (sub 1 (neg 2))

1+               !!> unexpected end of input
1 2              !!> unexpected NUM '2'
(1+2             !!> unexpected end of input
@                !!> unexpected character "@"
